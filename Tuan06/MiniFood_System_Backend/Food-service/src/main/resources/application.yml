server:
  port: 8082

spring:
  application:
    name: food-service
  profiles:
    active: dev


management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO