server:
  port: 8083

spring:
  application:
    name: order-service
  profiles:
    active: dev


management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO