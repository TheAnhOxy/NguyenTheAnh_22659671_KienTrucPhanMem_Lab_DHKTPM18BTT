server:
  port: 8084

spring:
  application:
    name: payment-service
  profiles:
    active: dev


management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO