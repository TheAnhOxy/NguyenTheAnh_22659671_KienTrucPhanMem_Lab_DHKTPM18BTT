server:
  port: 8082

spring:
  application:
    name: food-service
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5434/food_db?options=-c%20timezone=UTC
    username: root
    password: Theanh123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO